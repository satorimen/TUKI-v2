# ПЛАН РЕАЛИЗАЦИИ TUKI — платформа-сваха для ремонта (Израиль)

> Утверждён 2026-08-24. Документ живой: отмечайте выполненные пункты.
> Связанные документы: `PRODUCT_PRD.md` (полный PRD, исходный рынок РФ), `PIVOT_ISRAEL.md` (что изменилось для Израиля).

## Зафиксированные решения

| Параметр | Решение |
|----------|---------|
| Продукт | Reverse-marketplace: клиент описывает задачу ИИ-ассистенту → мастера предлагают → выбор → контакт передаётся |
| Модель | «Сваха»: платформа сводит стороны, далее сделка идёт в WhatsApp напрямую |
| Рынок | Израиль, вся страна через 4 гео-кластера (Гуш-Дан, Иерусалим, Хайфа+Север, Беэр-Шева+Юг) |
| Языки | Иврит (RTL, основной) + русский + английский. ИИ определяет язык автоматически |
| Деньги/телефоны | ₪ (ILS), формат +972, wa.me/972... |
| Монетизация | Фаза 1: бесплатно → Фаза 2: SaaS-подписка мастеров (вне скоупа MVP) |
| Стек | Next.js 14 + TypeScript + Tailwind + Supabase + Gemini + Cloudflare R2 |
| Бюджет | 0 ₽/мес (~500-1000 пользователей) |

---

## M1: Фундамент ✅ (ключи НЕ нужны)

- [x] Проверить окружение (node ≥18, npm) — Node v20.20.2, npm 10.8.2
- [x] `git init`, `.gitignore`, README
- [x] Инициализация Next.js 14 + TS + Tailwind + ESLint
- [x] i18n: next-intl, локали he/ru/en, RTL для иврита, шрифт Heebo
- [x] Сохранить план в `PLAN.md` + `PIVOT_ISRAEL.md`
- [x] Таблица городов Израиля (`src/lib/geo/cities.ts`): 56 городов → 4 кластера

**Результат:** приложение с рабочим переключением языков и RTL. Первый коммит.

## M2: ИИ-ассистент ✅ (провайдер mock; Gemini подключается ключом)

- [x] `AIProvider` интерфейс: `parse(input) → ParseResult` (тип работ, площадь, бюджет ₪, сроки, город, уточняющие вопросы, разбиение на подзадачи)
- [x] `MockProvider` — детерминированный rule-based парсер (он же fallback при сбое Gemini)
- [x] `GeminiProvider` — мультиязычные промпты (he/ru/en), JSON-выход с валидацией, PII-очистка, fallback на mock
- [x] API `POST /api/ai/parse` + фабрика провайдеров (GEMINI_API_KEY → gemini, иначе mock)
- [x] Страница `/new-task`: чат, карточка ТЗ, выбор города, публикация (localStorage до M3)
- [x] Тест-набор `.test/req-*.json`: he/ru/en + комплексная + multi-turn — все проходят

**Проверено:** категории/города (incl. склонения и ивритские префиксы)/бюджет (₪, שקל, «до N», диапазон)/площадь/сроки извлекаются; уточняющие вопросы ≤2; multi-turn сохраняет черновик.

## M3: Данные и пользователи ✅ (memory-режим; Supabase включается ключами)

- [x] SQL-миграция `supabase/migrations/001_initial_schema.sql`: profiles, masters, tasks, bids, reviews + RLS + индексы + триггер пересчёта рейтинга (веса из PRD §8.5)
- [x] Repository-абстракция `src/lib/db/`: интерфейс + MemoryDb (globalThis singleton) + SupabaseDb (маппинг camelCase↔snake_case, GIN-overlaps запросы)
- [x] Auth: cookie-сессии (HMAC httpOnly), email + 6-значный код (`/api/auth/*`), нормализация WhatsApp +972
- [x] Онбординг мастера `/master`: специализации-чипы, города по кластерам, опыт, bio, WhatsApp
- [x] Публикация заявок через `POST /api/tasks` (guard 401, инлайн-логин на /new-task)
- [x] E2E-тесты: регистрация мастера, публикация задачи клиентом, листинг, auth-guard — все зелёные

**Подключение Supabase (когда будут ключи):** выполнить миграцию в SQL Editor → добавить `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` в `.env.local` → Repository переключится автоматически.

## M4: Маркетплейс-ядро ✅

- [x] Матчер `src/lib/matching/matcher.ts`: город → специализация → кластер → соседние кластеры (MIN_MASTERS_PER_TASK=3); `feedForMaster` — лента по кластерам мастера
- [x] Лента мастера `/master/feed` + API `GET /api/master/feed` (свои отклики скрываются)
- [x] Отклики `POST /api/bids`: guard дубликата (409), лимит 10 pending (429), отклик только на published (409)
- [x] Клиентский вид `GET /api/tasks/[id]`: отклики + карточки мастеров + ранжирование «Лучшее соответствие» (рейтинг↓, цена↑); мастер видит только свой отклик (анти-демпинг)
- [x] Выбор исполнителя `POST /api/tasks/[id]`: bid→selected, остальные→rejected, task→assigned, wa.me-ссылки с префилл-сообщением на языке задачи
- [x] In-app уведомления: колокольчик (polling 30с), new_bid клиенту, bid_selected мастеру
- [x] E2E: публикация → 2 отклика → дубликат 409 → выбор → WhatsApp-хэндофф → уведомления — все зелёные

**Отложено (см. риски):** фото в R2 (нужны ключи Cloudflare) — PHOTO URL-поле в схеме готово.

## M5: Доверие и управление ✅

- [x] Отзывы `POST /api/reviews`: 5 критериев (качество 35% / смета 25% / пунктуальность 15% / чистота 15% / коммуникация 10% — веса PRD §8.5), guard'ы: только клиент задачи (403), оценка 1-5 (400), дубликат (409)
- [x] Завершение сделки `PUT /api/tasks/[id]` (кнопка «Работа выполнена»); отзыв авто-завершает сделку
- [x] Пересчёт рейтинга: MemoryDb — в коде, Supabase — SQL-триггером (оба по единым весам)
- [x] UI отзыва: 5×5 звёзд + текст на странице задачи; после отправки — благодарность
- [x] Админка `/admin` + `GET /api/admin/stats`: воронка (published→assigned→completed), пользователи/мастера/отклики/отзывы, топ-10 мастеров; доступ по ADMIN_EMAILS (default admin@tuki.local), не-админ → 403
- [x] E2E: завершение → отзыв → рейтинг 4.6 (взвешенный по формуле) → дубликат 409 → админ-статистика → 403 для не-админа

**Отложено:** email через Resend (нужен ключ — добавим при деплое), напоминание «состоялась ли сделка?» через 7 дней (нужен cron — кандидат на M7/Vercel Cron).

## M6: Запуск ✅ (кроме самого деплоя — нужен аккаунт Vercel)

- [x] PWA: `manifest.ts` (RTL, he, standalone, theme #f97316) + статические PNG-иконки 192/512
  (⚠️ рантайм-иконки next/og несовместимы с кириллическим путём проекта — сгенерированы Node-скриптом)
- [x] SEO: локализованные metadata (he/ru/en), `robots.txt` (api/admin закрыты), `sitemap.xml` (12 URL), viewport/themeColor
- [x] Аналитика: @vercel/analytics (активируется на Vercel автоматически)
- [x] README: пошаговый деплой на Vercel + подключение Supabase
- [x] Финальный QA-smoke: 8 страниц (200), манифест/робots/sitemap/иконки (200), полный API-цикл публикация→отклик→выбор(wa.me)→отзыв(рейтинг 4.85) — зелёные

**Для запуска в продакшен осталось (владелец):** залить на GitHub → импорт в Vercel → env: `GEMINI_API_KEY`, `AUTH_SECRET`, `ADMIN_EMAILS`, `NEXT_PUBLIC_SITE_URL` (+ Supabase для персистентности — иначе память обнуляется при redeploy).

## M7 (опционально): WhatsApp-бот

- [ ] Baileys/WAHA на отдельном номере: уведомления, отклики из WA, сбор отзывов
- [ ] При росте → официальный WhatsApp Business API

---

## Definition of Done MVP — статус

1. ✅ Клиент создаёт ТЗ через ИИ-чат на иврите/русском/английском (Gemini + мок-fallback)
2. ✅ Мастер регистрируется, видит релевантные ТЗ в своём кластере, откликается
3. ✅ Клиент выбирает исполнителя → обе стороны получают WhatsApp-контакт (wa.me с префилл-текстом)
4. ✅ Отзывы и рейтинги работают (5 взвешенных критериев)
5. ✅ Админка показывает воронку (published→assigned→completed)
6. ⏳ Деплой на Vercel — код готов, нужен аккаунт владельца (README §Деплой)

## Что и когда нужно от владельца продукта

| Когда | Что | Где взять |
|-------|-----|-----------|
| К M2 (не блокирует) | Gemini API key | aistudio.google.com, бесплатно |
| К M3 | Supabase-проект | supabase.com, бесплатно |
| К M6 | Vercel-аккаунт + (опц.) домен | vercel.com, бесплатно |
